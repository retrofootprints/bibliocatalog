// Tier 2 — Shelf photo OCR (SPEC §4.2, Phase 5).
//
// Deliberately out of scope for this build (Phases 1-2 only). This module is
// a placeholder so the folder layout matches SPEC §13.1 ahead of time; it is
// not imported anywhere, so it has zero impact on the Phase 1/2 bundle.
export {};
